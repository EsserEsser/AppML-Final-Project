[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "appliedml-final-project"
version = "0.1.0"
description = "Machine learning project for estimating population in aerial images"
requires-python = "">=3.12,<3.13""
